# M7 — Dashboard, reports and product integration

## Final acceptance continuation — 2026-09-26

- PR #7 remains open on `feat/m7-dashboard-reports-polish`; no new milestone or migration. Project financial figures navigate to the matching recorded, lifetime net-movement or current-outstanding report with project/type scope. Back preserves the project. The M6 project `collected`/`paid` amounts are net of refunds, so their labels now explicitly say **Net collected/Net paid**, matching the M7 destination net cards.
- The project picker uses authorized server-side search and pages of 100, exposes load more, and loads the selected project separately when it is outside the page. Historical report selection retains archived accessible projects; new assignment selectors retain active/complete rules. Flutter widget tests simulate 101 accessible projects and check page two/search/selection; PostgreSQL backend test checks scope-before-pagination and forbidden detail. The expanded native connected journey was interrupted by the normal development OTP rate limit; do not count it as a pass.
- Backend performance fixture: PostgreSQL 17.11 Docker test instance, 8 logical CPUs and 128 MB shared buffers; two isolated workspaces of 100 and 5000 entries, with income, general/shared/single expenses, settlements and every-fifth refunds. Three sequential HTTP timings in milliseconds (first run may include warmup):

| 5000 entries, report | Before | After |
| --- | --- | --- |
| Dashboard | 43.0, 43.9, 44.3 | 34.7, 30.5, 40.6 |
| Recorded | 80.3, 103.7, 114.4 | 36.6, 32.3, 29.6 |
| Movements, global | 2098.1, 812.9, 732.6 | 10.6, 9.6, 9.3 |
| Outstanding, global | 683.6, 693.0, 722.4 | 14.0, 14.0, 13.3 |
| Movements, equipment A | 1568.6, 1787.9, 1728.0 | 719.7, 673.2, 657.8 |
| Outstanding, equipment A | 1411.2, 1518.4, 1472.2 | 602.4, 600.2, 590.9 |

  On 100 entries after change: dashboard 153.5/16.4/10.7, recorded 9.6/8.2/7.1, global movements 13.7/10.5/7.1, global outstanding 7.7/4.7/4.9, equipment movements 29.4/23.5/22.4, equipment outstanding 23.1/18.1/17.0 ms. Baseline 100: dashboard 111.3/12.0/9.4, recorded 15.9/8.8/7.1, global movements 37.7/25.9/22.4, global outstanding 25.6/17.9/17.2 ms. These are local measurements, not SLA or p95.
- Query counts were inferred from code, not instrumented: filtered movements on this 5000 fixture previously implied about 3000 per-entry allocation lookups, now at most 18 allocation batches (256 entries/batch); filtered outstanding about 2500, now at most 15 batches. Global movements/outstanding use one aggregate/count and one paged-detail SQL query, while dashboard/recorded stream matching rows with bounded fetch size. A representative 5000-entry movement aggregate `EXPLAIN (ANALYZE, BUFFERS)` read 6000 movement rows, hit 217 shared buffers, and executed in 9.754 ms. Equipment-filtered reports still take roughly 0.6–0.7 seconds locally at 5000; further scaling may need separate work, without changing M2 allocation rounding.
- Independent 5000-entry fixture reconciliation expected and API actual: income 125000.00, expense 375000.00, net collected 70000.00, net paid 210000.00, receivable 55000.00, payable 165000.00; equipment A expense 200000.00/payable 88000.00, B expense 50000.00/payable 22000.00, general expense 125000.00. Separate M7 regression verifies expense/income 1000.00 with settlement 600.00 then next-month refund 200.00, including negative period net, lifetime net 400.00 and current remaining 600.00. Existing tests cover independent equal amounts, attachments, shared rounding, exclusions and cross-workspace scope.
- Populated UI: a normal dev OTP login in a temporary Flutter iOS integration test against the isolated acceptance API/PostgreSQL passed 1/1 and captured actual rendered screens at Arabic compact 390, English compact 390 and desktop 1280, Urdu compact 390. Inspected Home, recorded, October movements (negative −200.00), current outstanding, project detail, attention and representative dates/shares. One English refund label was corrected from Arabic. This does **not** establish a complete browser or project→entry→Back acceptance pass. Browser control could not supply login input, Chrome control was unavailable, and expanded native attempts hit OTP throttling; empty/error UI is widget-tested but not visually confirmed against the populated API.
- Short remaining manual walkthrough: start the existing isolated test Postgres (`docker compose -f infra/compose.yaml --profile test up -d`), then at repo root run `DB_URL=jdbc:postgresql://127.0.0.1:55433/equipment_m7_acceptance DB_USER=equipment_test DB_PASSWORD=isolated-test-only SERVER_PORT=8090 OBJECT_ROOT=../.local/m7-acceptance-objects JAVA_HOME=$(/usr/libexec/java_home -v 21) ./scripts/backend-dev.sh`. In `app`, run `flutter run -d chrome --web-port=8081 --dart-define=API_BASE_URL=http://127.0.0.1:8090/api/v1`. Log in as synthetic owner `0500000501` with development OTP `123456` after its normal cooldown. At 390–400 and desktop width in ar/en, inspect Home, September recorded (expense 2250.00, income 1000.00), October movements (net paid/collected −200.00), current outstanding (payable 1850.00, receivable 600.00); inspect an Urdu compact recorded report. From a project net figure, open report→original entry→Back; search/select `M7 Search 001` (outside first 100), then check empty/error states by a no-result search and ordinary network interruption/retry. Confirm no clipping and signed money/date/direction. The acceptance DB is isolated synthetic data; do not reset it or bypass auth.
- Required verification on final code: full backend/Flutter suites, Flutter analyze/web release, OpenAPI structural check, `git diff --check`, and PR CI at final SHA. Existing production exclusions (OTP, object storage/scanning, Push), Android and native Urdu review remain outside this acceptance task.

Status: locally implemented and verified with the limits below, 2026-09-26; PR #7 open, backend and Flutter CI passed on `7313002`.

- Authority: owner's M7 task attached to this session; written scope governs the visual reference at `docs/design/m7-dashboard-reports-polish-wireframe.png`.
- Base: `main` and `origin/main` matched at `11afef6045c9ef62d6daa25f437569cf007d8a7f` after `git pull --ff-only origin main`. M6 merge, CI workflow and Flyway V21 were present.
- Branch: `feat/m7-dashboard-reports-polish`. The owner's previously untracked wireframe was preserved for this branch.
- Agent work: one Implementer completed backend, Flutter, and the independently reviewed shared-movement allocation correction in sequential stages. Main agent owned documentation and final verification. One UX review and one security/financial review were performed sequentially; no parallel code writers or nested agents.
- Backend: `/dashboard`, `/reports/recorded`, `/reports/movements`, `/reports/outstanding` implemented with matching paged detail and source totals; 8 focused PostgreSQL tests and 89/89 full backend tests pass, 0 skipped. OpenAPI structural parsing reported 98 paths and 407 valid local references. No new migration is needed.
- Connected checkpoint: a separate synthetic `equipment_m7_acceptance` database in the test PostgreSQL container applied V1–V21 fresh. Its local API on port 8090 passed a populated journey: owner plus selected-organization member and driver; general/shared expenses, income/expense across September/October settlement/refund dates, cancelled entry, draft, pending submission, project classification, document/issue attention. Owner recorded September expense 2250.00/income 1000.00; October period net paid/collected -200.00; current payable 1850.00/receivable 600.00. Equipment shares 1700.00/500.00 and general 50.00 reconciled. Restricted member saw only eligible originals and driver report call returned 404. This is API-connected evidence, not browser or native-device evidence.
- Review: UX found first-use placement, date display, and an empty-state issue; fixes passed Flutter tests. Independent security/financial review found shared-expense movement attribution after a refund; correction and fractional regression test passed. The review's query-scaling concern remains: summaries scan every eligible row per request; filtered shared movements still fetch allocation per row. Outstanding removed its per-row finance.require call.
- Final gates: Flutter 98/98, analyze clean, Web release build pass. Existing iOS integration suite 3/3 passed on iPhone 17 Pro simulator against the isolated acceptance API after updating stale test expectations for M2 total locking, current navigation and localized money; no M7-specific native screen test is claimed. Connected populated API journey passed. Attempted populated Web visual on IAB and Chrome: browser control could focus the Flutter phone input but supplied text remained empty, so login and subsequent responsive screen checks were unavailable. Android unavailable/not run. PR #7 is open; backend and Flutter PR CI both passed on `7313002`. Web visual acceptance remains open.
- Required follow-up gates: browser/manual populated visual at 390–400px and desktop in ar/en/ur, Urdu native-speaker linguistic review, long-history query performance validation, PR/CI review, and release/production work outside M7.
- Production exclusions: OTP, object storage/scanning, Push, account recovery/deletion, deployment/operations, and native Urdu review remain pending. M7 does not start any later milestone.
- Commits before PR: backend/API `0c85b83`, Flutter/integration `8d50c46`, documentation `240a029`, all based on `11afef6`. The feature branch is pushed to origin. PR: https://github.com/MuathMunif/equipment-platform/pull/7. GitHub CLI creation was denied for its signed-in account; the existing signed-in owner browser session created the PR without changing authentication.
- Next action: arrange the remaining populated Web visual pass before accepting M7. Do not merge automatically.
