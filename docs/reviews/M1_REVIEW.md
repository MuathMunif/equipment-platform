# مراجعات الشريحة الأولى

## BASE-001 — 2026-09-24
المراجع المستقل: agent `/root/review` بدور reviewer، قراءة فقط. الكاتب: `/root/implement`؛ القائد سجل هذا التقرير.

النطاق: pom، profiles، migration V1، common/Idempotency/Values/DevelopmentBoundary، audit، compose.

النتيجة: لا finding مؤكد مانع لـM1 في الدفعة الأساسية وحدها. المراجع تحقق من exact decimals، مفاتيح العلاقات المركبة لمساحة العمل، فصل قواعد dev/test وربط المنافذ بـloopback، وحد رفض تشغيل بديل التطوير خارج profile صريح.

متابعة مطلوبة عند ظهور الخدمات:
- التحقق من أن كل مستدعٍ لـIdempotency يجري تفويضًا حاليًا وtransaction قبل replay.
- عدم تثبيت دفعة واحدة كقاعدة منتج؛ القائد طلب إزالة UNIQUE(entry_id) من schema قبل أول migration، مع اختبار أن shortcut M1 ينشئ تسوية واحدة ذريًا. الدفعات المتعددة من M2.
- OWNER/POSTED هما نطاق M1 الحالي، وليس تنفيذًا للفريق أو المراجعة.

الاختبارات من المراجع: NOT RUN (قراءة فقط؛ لا تشغيل build/cache/db writes). هذه مراجعة مستقلة للكود وليست دليل نجاح التنفيذ.

## مراجعة UX المسبقة — 2026-09-24
المراجع المستقل: `/root/ux` بدور ux_reviewer. قرأ الفلو والمهمة والمراجع الشكلية ومهارة كتابة UX العربية؛ لم تكن لقطات التطبيق الفعلي متاحة بعد.

أرسل للكاتب ضوابط: دخول تطويري معلن، اسم المستخدم الجديد مرة واحدة، فراغ مع إجراء إضافة، اسم وموديل نصي فقط، مصروف يحدد المعدة من السياق، حالة «دفعته كاملًا» ظاهرة، حفظ مالي منفصل عن الرفع مع «حُفظ المصروف، وتعذر رفع المرفق»، إعادة المرفق وحده، حماية المدخلات، منع النقر المكرر، فصل الإجمالي والمدفوع والمتبقي، RTL واستجابة دون وظائف وهمية.

مراجعة التطبيق الفعلي النهائية ونتائج إصلاح الملاحظات: PENDING.
