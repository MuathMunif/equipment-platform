# M2 — مصروف جزئي أو غير مدفوع ودفعات مؤرخة

الحالة: شريحة محلية منفذة في 2026-09-24. بقية M2 وV1 غير مكتملة.

## النطاق
- إنشاء مصروف كامل/جزئي/غير مدفوع من واجهة عربية؛ الجزء الأول دفعة في الأصل نفسه.
- اسم الطرف إلزامي عند وجود متبقٍ، والموعد اختياري. الحالة والرصيد محسوبان من الدفعات المحفوظة.
- إضافة دفعات لاحقة مؤرخة إلى المصروف نفسه، مع منع الدفع الزائد والتكرار والتسوية المتزامنة الزائدة.
- Flyway V2 تضيف `party_name` و`due_date` دون تعديل سجلات M1 القديمة؛ عقد OpenAPI محدّث.
- نافذة إضافة الدفعة قابلة للتمرير على iOS، ويظل حقل المبلغ صالحًا حتى اكتمال إغلاق النافذة بعد الحفظ.

## التحقق
- PASS: `MAVEN_USER_HOME=/Users/muath/Desktop/equipment-platform/.local/maven-user JAVA_HOME=/Users/muath/Library/Java/JavaVirtualMachines/ms-21.0.7/Contents/Home ./mvnw -B -ntp -q -Dmaven.repo.local=/Users/muath/Desktop/equipment-platform/.local/m2 test` من `backend/`؛ 14 اختبار HTTP/PostgreSQL، بلا فشل، على قاعدة `equipment_test` المعزولة. يشمل FIN-02/03/05/06/08 للمصروف.
- PASS: `dart format lib/main.dart test/widget_test.dart`, `flutter analyze`, `flutter test` من `app/`؛ 8 اختبارات widget/unit، بلا فشل.
- PASS: تحليل YAML لعقد OpenAPI بواسطة Ruby Psych، و`git diff --check`.
- PASS (تحقق إضافي 2026-09-24): `flutter build web --release --dart-define=API_BASE_URL=http://127.0.0.1:8082/api/v1`، ثم استخدام النسخة المبنية على `127.0.0.1:8083` بعرض متصفح ضيق مع API الحالي على 8082 وPostgreSQL dev. عبر واجهة المالك الحقيقية: مصروف 350.00 بدفعة أولى 100.00 وطرف «مورد اختبار M2»؛ أظهرت التفاصيل المتبقي 250.00. دفعة 150.00 جعلت المتبقي 100.00؛ رفض الخادم 100.01 برسالة «الدفعة أكبر من المتبقي»؛ دفعة 100.00 جعلت المدفوع 350.00 والمتبقي 0.00 وثلاث دفعات داخل أصل واحد. بعد تحديث الصفحة ظهر الأصل والدفعات الثلاث والحالة نفسها. إنشاء مصروف آخر غير مدفوع 70.00 رفض الحفظ بلا اسم طرف، ثم حُفظ باسم طرف وأظهر 0.00 مدفوعًا و70.00 متبقيًا ودون أي دفعة أو موعد استحقاق.
- PASS (تحقق إضافي): فحص فعلي عند عرض 1280×900: عرض سجل المعدة والحالة غير المدفوعة، فتح تفاصيل مصروف 70.00، إضافة 20.00 فأصبحت الحالة جزئية والمتبقي 50.00. بعد إعادة بناء الويب بالإصلاح وإعادة تحميل الصفحة، أضيفت دفعة 10.00 وأظهر السجل 30.00 مدفوعًا و40.00 متبقيًا دون تكرار أصل المصروف.
- PASS (تحقق إضافي): `flutter test integration_test/owner_flow_test.dart -d 657C142E-E597-481F-B8F9-8C07082C1D30 --dart-define=API_BASE_URL=http://127.0.0.1:8082/api/v1`؛ اختبار iOS simulator واحد نجح مع API/PostgreSQL فعليين. توسع الاختبار ليغطي إنشاء مصروف جزئي 350.00 بدفعة أولى 100.00 واسم طرف من واجهة iOS، ثم دفعة 250.00 من نافذة التفاصيل؛ أكد API أصلًا واحدًا جديدًا، دفعتين، ومتبقيًا 0.00. يحتفظ اختبار M1 القديم بفحص المرفق واستعادة الجلسة.
- فشل اختبار iOS الأول بعد التوسعة لأن `TextEditingController` في نافذة الدفعة أُتلف قبل انتهاء إغلاقها؛ أُزيلت ملكية المتحكم المؤقتة وجُعلت النافذة قابلة للتمرير، ثم نجح تشغيل الاختبار كاملًا. بعد الإصلاح نجح `flutter analyze` و`flutter test` (8 اختبارات) و`flutter build web --release`.
- NOT RUN: جهاز جوال فعلي، Android native، مزود S3 خاص وفحص البرمجيات الخبيثة. اختبار widget يستخدم MockClient؛ اختبارات الخادم تستخدم PostgreSQL وHTTP فعليين.

الخدمتان المؤقتتان 8082/8083 أوقفتا بعد التحقق. بقيت عمليتا 8080/8081 السابقتان دون تغيير. لم تُحذف بيانات التطوير أو تُعاد تهيئة قاعدتها.

## متابعة
أول عمل مناسب بعد هذه الشريحة: مهمة دخل جزئي/غير مستلم مستقلة، ثم بقية عناصر M2 والتخزين الخاص. لا يُعد M2 مكتملًا قبل بقية عناصر القائمة وتكامل التخزين المختار.
