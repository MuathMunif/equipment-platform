# M2 — الإيراد والتحصيلات المؤرخة

الحالة: شريحة محلية منفذة في 2026-09-24. بقية M2 وV1 غير مكتملة.

## النطاق
- نوع `INCOME` في أصل العملية المالية عبر Flyway V3. سجلات المصروف السابقة تبقى `EXPENSE`، ولا تُنشأ عملية ثانية عند إضافة تحصيل.
- إنشاء إيراد مستلم بالكامل، مستلم جزئيًا، أو غير مستلم. يُطلب اسم الطرف عند وجود متبقٍ، والموعد اختياري. التحصيل الأول واللاحق مرتبطان بالأصل نفسه، والتواريخ محفوظة.
- مبالغ SAR عشرية دقيقة؛ منع تجاوز المتبقي وإعادة الطلب المكرر، مع بقاء عزل مساحة العمل وتفويض الخادم. تستخدم الواجهة تسميات الإيراد والتحصيل، ويبقى `paidOn`/`initialPaid` في عقد JSON للتوافق.
- عرض الإيراد والتحصيلات داخل سجل المعدة وتفاصيل العملية في Flutter عربي RTL. لا توجد ملخصات دخل/مصروف حسب الفترة في هذه الشريحة.

## التحقق
- PASS: `MAVEN_USER_HOME=/Users/muath/Desktop/equipment-platform/.local/maven-user JAVA_HOME=/Users/muath/Library/Java/JavaVirtualMachines/ms-21.0.7/Contents/Home ./mvnw -B -ntp -q -Dmaven.repo.local=/Users/muath/Desktop/equipment-platform/.local/m2 test` من `backend/` على قاعدة `equipment_test`؛ 16 اختبارًا، 0 فشل. تشمل تجربة إيراد 3000.00 بتحصيل 1000.00 في 20 سبتمبر و2000.00 في 2 أكتوبر، أصلًا واحدًا وتحصلين، وإعادة الطلب والتعارض، ورفض تحصيل زائد والوصول عبر مساحة أخرى.
- PASS: `flutter analyze` و`flutter test` من `app/`؛ 9 اختبارات دون فشل، منها نموذج إيراد جزئي.
- PASS: `flutter test integration_test/owner_flow_test.dart -d 657C142E-E597-481F-B8F9-8C07082C1D30 --dart-define=API_BASE_URL=http://127.0.0.1:8082/api/v1`؛ اختبار iOS simulator واحد يمر عبر واجهة Flutter وAPI/PostgreSQL فعليين. أنشأ إيراد 3000.00 بتحصيل 1000.00 ثم 2000.00، وتأكد من بقاء ثلاثة أصول فقط للمعدة التجريبية، وتحصلين للإيراد ورصيد صفر.
- PASS: `flutter build web --release --dart-define=API_BASE_URL=http://127.0.0.1:8082/api/v1`. فحص ويب حي على 8083 مع API على 8082 وPostgreSQL dev: رفض حفظ إيراد 500.00 غير مستلم دون اسم طرف، ثم حفظه باسم «عميل اختبار M2»؛ عرض 0.00 مستلم و500.00 متبقٍ، وبعد تحصيل 200.00 عرض 200.00 مستلم و300.00 متبقٍ وتحصيلًا واحدًا داخل الأصل.
- PASS: تحليل عقد OpenAPI بـRuby Psych و`git diff --check`.

## الحدود
- لا Android native أو جهاز فعلي في هذا التحقق. تخزين S3 الخاص وفحص البرمجيات الخبيثة وOTP الإنتاجي لم تُفعّل. API/الويب على 8082/8083 للتطوير المؤقت فقط، ولم يُجر اختبار إصدار إنتاجي.
- بقية M2: التعديل المسموح، البحث/التصفية الموسعة، مسودة الصورة واستكمالها، ومسار التخزين الخاص المختار. لا تُعد M2 مكتملة.
