# Project-local, non-secret settings. Validate support in the installed Codex version.
# Do not replace user/global settings or weaken sandbox/approval policy.
# Project defaults; an existing conversation may retain explicit session overrides.
model = "gpt-6-sol"
model_reasoning_effort = "low"

[agents]
max_concurrent_threads_per_session = 1
default_subagent_reasoning_effort = "low"
